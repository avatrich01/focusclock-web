// Missed-hours recovery was removed when hours became auto-counted from the
// schedule (no manual check-ins to miss). Kept as a no-op to avoid dead imports.
export function RecoveryModal(): null {
  return null
}
